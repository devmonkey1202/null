[package]
name = "ffi-wasm-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
core-error = { path = "../core-error" }
kernel-runtime = { path = "../kernel-runtime" }

[lib]
path = "src/lib.rs"

